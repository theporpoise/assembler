#include <stdio.h>
#include <string.h>
#include <ctype.h>

// rewrite using printf and scanf since, getchar not practical b/c have
// to track a bunch of previous values and have crazy if / else statements[


main()
{
    FILE *fp;
    //FILE *fopen(char *name, char *mode);
    fp = fopen("b.hack", "r");

    

  

    return 0;
}





:

